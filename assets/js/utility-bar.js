document.addEventListener('DOMContentLoaded', () => {
  /* ---------- DATA ---------- */

  const cities = [
    { city: "Kyiv", temp: "3°C", weather: "☁️ Cloudy", alert: "🟡 Elevated Risk", photo: "/assets/images/cities/kyiv.jpg", oblastId: "oblast-kyiv-city" },
    { city: "Lviv", temp: "-1°C", weather: "❄️ Snow", alert: "🟡 Elevated Risk", photo: "/assets/images/cities/lviv.jpg", oblastId: "oblast-lviv" },
    { city: "Kharkiv", temp: "0°C", weather: "🌫️ Fog", alert: "🔴 Critical", photo: "/assets/images/cities/kharkiv.jpg", oblastId: "oblast-kharkiv" },
    { city: "Odesa", temp: "4°C", weather: "🌧️ Rain", alert: "🟠 High Risk", photo: "/assets/images/cities/odesa.jpg", oblastId: "oblast-odesa" },
    { city: "Mykolaiv", temp: "2°C", weather: "☁️ Cloudy", alert: "🟡 Elevated Risk", photo: "/assets/images/cities/mykolaiv.jpg", oblastId: "oblast-mykolaiv" },
    { city: "Zaporizhzhia", temp: "1°C", weather: "🌤️ Partly Cloudy", alert: "🟡 Elevated Risk", photo: "/assets/images/cities/zaporizhzhia.jpg", oblastId: "oblast-zaporizhzhia" }
  ];

  /* Daily autogenerated fact about Ukraine — one per day of year (index 0–365) */
  const ukraineFacts = [
    "Ukraine is home to the world’s deepest metro station (Arsenalna, Kyiv).",
    "Ukraine has seven UNESCO World Heritage sites, including Kyiv’s Saint-Sophia Cathedral.",
    "The Ukrainian language is one of the most widely spoken Slavic languages.",
    "Ukraine is often called the “breadbasket of Europe” for its grain production.",
    "Lviv’s historic centre is a UNESCO World Heritage site.",
    "Ukraine’s Carpathian Mountains are a key biodiversity hotspot.",
    "The country has a long tradition of folk embroidery, with regional patterns.",
    "Ukraine’s constitution was adopted in 1996.",
    "Odesa is known for its architecture and the Potemkin Stairs.",
    "Ukrainian literature includes Taras Shevchenko and Lesya Ukrainka.",
    "Ukraine has a strong tradition of choral and bandura music.",
    "The Trypillian culture in Ukraine dates back over 6,000 years.",
    "Ukraine’s Dnipro River is one of the major rivers of Europe.",
    "Kyiv’s Golden Gate is a historic gateway from the medieval city.",
    "Ukraine has contributed significantly to aerospace and IT industries.",
    "The pysanka (decorated egg) is a symbol of Ukrainian culture.",
    "The country has 24 oblasts plus Kyiv and Sevastopol.",
    "Ukrainian diaspora communities exist across the Americas and Europe.",
    "Ukraine’s nature reserves protect rare species like the European bison.",
    "The sunflower is an important national symbol.",
    "Ukraine’s IT sector has grown into a major export industry.",
    "Traditional Ukrainian cuisine includes borscht, varenyky, and salo.",
    "Ukraine’s flag combines blue (sky) and yellow (wheat).",
    "The Hryvnia is Ukraine’s currency.",
    "Ukrainian civil society has shown remarkable resilience.",
    "Ukraine’s Black Sea coast has important ports and ecosystems.",
    "Ukrainian volunteers have been central to humanitarian response.",
    "The country’s museums preserve art, history, and cultural heritage.",
    "Ukraine’s healthcare and education systems have deep historical roots.",
    "Ukrainian designers and craftspeople preserve traditional arts.",
    "The country’s legal profession supports justice and accountability.",
    "Ukraine’s women have been leaders in military, medicine, and civil society.",
    "Ukrainian musicians perform classical, folk, and contemporary music.",
    "The country’s architects plan reconstruction of cities and villages.",
    "Ukraine’s teachers continue to educate in shelters and online.",
    "Ukrainian poets and writers reflect on identity and resistance.",
    "The country’s engineers work on demining and reconstruction.",
    "Ukraine’s farmers have continued to plant and harvest under difficult conditions.",
    "Ukrainian filmmakers document the war and recovery.",
    "The country’s psychologists support trauma recovery.",
    "Ukraine’s hospitals and clinics provide care under fire.",
    "Ukrainian translators bridge languages for aid and diplomacy.",
    "The country’s mayors and local leaders coordinate recovery.",
    "Ukraine’s culture of mutual aid strengthens communities.",
    "Ukrainian runners and cyclists raise funds for aid.",
    "The country’s firefighters and rescuers save lives daily.",
    "Ukraine’s youth have driven innovation and civic engagement.",
    "Ukrainian artists document and respond to the war through art.",
    "The country’s libraries and archives preserve written heritage.",
    "Ukraine’s railways help evacuate people and deliver aid.",
    "Ukrainian chefs and restaurants keep culinary traditions alive.",
    "The country’s NGOs coordinate with international aid agencies."
  ];

  /* Humanitarian/aid Ukraine news: loaded from assets/data/ukraine-news.json (EN + UK, rotates). Fallback below. */
  const newsSnippetsFallback = [
    { title_en: "Local NGOs deliver heating equipment to families in Lviv oblast.", title_uk: "Місцеві НУО доставляють опалювальне обладнання сім'ям у Львівській області.", url: "/programs/humanitarian", category: "humanitarian" },
    { title_en: "Ukrainian volunteers run community kitchens in Odesa.", title_uk: "Українські волонтери організовують громадські кухні в Одесі.", url: "/programs/humanitarian", category: "civil-society" },
    { title_en: "Mental health support expands for displaced children in Kyiv region.", title_uk: "Розширюється підтримка психологічного здоров'я для переміщених дітей у Київському регіоні.", url: "/programs/resilience", category: "humanitarian" }
  ];

  /* ---------- ELEMENTS ---------- */

  const cityPhotoEl = document.getElementById('city-photo');
  const cityTextEl = document.getElementById('city-spotlight-text');
  const rotationCta = document.getElementById('rotation-cta');
  const factOfDayEl = document.getElementById('fact-of-day');
  const newsSnippetEl = document.getElementById('news-snippet');
  const mapTooltip = document.getElementById('map-tooltip');
  const timeKyivEl = document.getElementById('time-kyiv');
  const timeBremertonEl = document.getElementById('time-bremerton');
  const oblastEls = document.querySelectorAll('.oblast');

  /* ---------- LIVE CLOCKS: Kyiv (Europe/Kyiv), Bremerton (America/Los_Angeles) ---------- */

  const timeOpts = { hour: '2-digit', minute: '2-digit', hour12: false };

  function updateClocks() {
    const now = new Date();
    const kyivTime = now.toLocaleTimeString('en-GB', { ...timeOpts, timeZone: 'Europe/Kyiv' });
    const bremertonTime = now.toLocaleTimeString('en-GB', { ...timeOpts, timeZone: 'America/Los_Angeles' });
    if (timeKyivEl) {
      timeKyivEl.textContent = kyivTime;
      timeKyivEl.setAttribute('datetime', now.toISOString());
    }
    if (timeBremertonEl) {
      timeBremertonEl.textContent = bremertonTime;
      timeBremertonEl.setAttribute('datetime', now.toISOString());
    }
  }

  updateClocks();
  setInterval(updateClocks, 1000);

  /* ---------- CITY ROTATION + MAP HIGHLIGHT ---------- */

  let cityIndex = 0;

  function setActiveOblast(oblastId) {
    oblastEls.forEach(o => o.classList.remove('oblast-active'));
    if (!oblastId) return;
    const active = document.getElementById(oblastId);
    if (active) active.classList.add('oblast-active');
  }

  function rotateCity() {
    if (!cityPhotoEl || !cityTextEl) return;
    const data = cities[cityIndex];
    cityPhotoEl.src = data.photo;
    cityPhotoEl.alt = data.city + " photo";
    cityTextEl.textContent = `${data.city} — ${data.temp} • ${data.weather} • ${data.alert}`;
    setActiveOblast(data.oblastId);
    cityIndex = (cityIndex + 1) % cities.length;
  }

  /* ---------- CTA (single; optional rotation of link) ---------- */

  const ctaLinks = [
    { text: "How You Can Help Today", href: "/donate" },
    { text: "Donate to Humanitarian Aid", href: "/donate" },
    { text: "Support Legal Research", href: "/programs/legal-research" },
    { text: "Volunteer with WSUA", href: "/volunteer-apply" }
  ];
  let ctaIndex = 0;
  function rotateCta() {
    if (!rotationCta) return;
    const cta = ctaLinks[ctaIndex];
    rotationCta.textContent = cta.text;
    rotationCta.href = cta.href;
    ctaIndex = (ctaIndex + 1) % ctaLinks.length;
  }
  rotateCta();
  setInterval(rotateCta, 8000);

  /* ---------- DAILY FACT (autogenerated by day of year) ---------- */

  function getFactOfDay() {
    const start = new Date(new Date().getFullYear(), 0, 0);
    const now = new Date();
    const dayOfYear = Math.floor((now - start) / (1000 * 60 * 60 * 24));
    const index = dayOfYear % ukraineFacts.length;
    return ukraineFacts[index];
  }

  function renderFactOfDay() {
    if (!factOfDayEl) return;
    const fact = getFactOfDay();
    factOfDayEl.innerHTML = `<span class="fact-label">Fact of the day:</span> ${fact} <a href="/about">Learn more →</a>`;
  }

  /* ---------- NEWS SNIPPET: fetch real feed, rotate EN ↔ UK ---------- */

  let newsItems = [];
  let newsIndex = 0;
  let showUkrainian = false;

  function renderNewsSnippet() {
    if (!newsSnippetEl) return;
    if (newsItems.length === 0) {
      newsSnippetEl.innerHTML = `<span class="news-label">Ukraine:</span> <a href="/programs/humanitarian">Loading news…</a>`;
      return;
    }
    const item = newsItems[newsIndex];
    const title = showUkrainian ? (item.title_uk || item.title_en) : (item.title_en || item.title_uk);
    const lang = showUkrainian ? 'uk' : 'en';
    newsSnippetEl.innerHTML = `<span class="news-label">Ukraine:</span> <a href="${item.url}" target="_blank" rel="noopener" hreflang="${lang}">${escapeHtml(title)}</a>`;
    showUkrainian = !showUkrainian;
    if (!showUkrainian) newsIndex = (newsIndex + 1) % newsItems.length;
  }

  function escapeHtml(s) {
    const div = document.createElement('div');
    div.textContent = s;
    return div.innerHTML;
  }

  renderNewsSnippet();
  fetch('/assets/data/ukraine-news.json')
    .then(r => r.ok ? r.json() : Promise.reject())
    .then(data => {
      if (data && Array.isArray(data.items) && data.items.length > 0) {
        newsItems = data.items;
      } else {
        newsItems = newsSnippetsFallback;
      }
      renderNewsSnippet();
    })
    .catch(() => {
      newsItems = newsSnippetsFallback;
      renderNewsSnippet();
    })
    .finally(() => {
      setInterval(renderNewsSnippet, 8000);
    });

  /* ---------- MAP CLICK + TOOLTIP ---------- */

  if (oblastEls.length) {
    oblastEls.forEach(oblast => {
      oblast.addEventListener('click', () => {
        const region = oblast.dataset.region;
        if (!region) return;
        const oblastId = 'oblast-' + region;
        const city = cities.find(c => c.oblastId === oblastId);
        if (city) {
          cityIndex = cities.indexOf(city);
          rotateCity();
        } else {
          window.location.href = '/programs/humanitarian';
        }
      });

      oblast.addEventListener('mousemove', (e) => {
        const name = oblast.dataset.name || "Ukraine Region";
        mapTooltip.textContent = name;
        mapTooltip.style.left = e.clientX + "px";
        mapTooltip.style.top = e.clientY + "px";
        mapTooltip.style.opacity = "1";
      });

      oblast.addEventListener('mouseleave', () => {
        mapTooltip.style.opacity = "0";
      });
    });
  }

  /* ---------- INIT ---------- */

  rotateCity();
  renderFactOfDay();

  setInterval(rotateCity, 6000);
});
